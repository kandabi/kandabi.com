{
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix ./frontend\" \"npm run dev --prefix ./backend\"",
    "copy-ts-files": "node ./src/copy-ts-files.js"
  },
  "devDependencies": {
    "concurrently": "^8.2.1"
  }
}
