import { theme, ITheme } from './theme';
import { colorConfig, Colors, IColor, HexColor } from './colors';
import { breakpoints, mediaSizes, Breakpoints } from './breakpoints';

export { theme, colorConfig, breakpoints, mediaSizes, Colors, Breakpoints };
export type { ITheme, IColor, HexColor };
